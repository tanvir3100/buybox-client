/* eslint-disable no-unused-vars */


const cardVolume = ({children}) => {
    return (
        <div className="w-full px-12 mx-auto">
            {children}
        </div>
    );
};

export default cardVolume;